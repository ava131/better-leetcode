# 复制为 .env 并填入。key 永远不进浏览器。
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_API_KEY=
# 默认模型
LLM_MODEL=deepseek-v4-pro
# 侧边栏下拉框里的可选项（逗号分隔）
LLM_MODELS=deepseek-v4-pro,deepseek-flash

# 后端监听端口（只绑 127.0.0.1）
PORT=8787

# 记忆库位置（默认 ~/.better-leetcode/memory.db）
# MEMORY_DB=/absolute/path/to/memory.db
